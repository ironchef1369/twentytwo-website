{
  "name": "twentytwo-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/src dist/assets && cp index.html dist/index.html && cp src/styles.css dist/src/styles.css && cp src/main.js dist/src/main.js && cp -R public/assets/. dist/assets/"
  }
}
